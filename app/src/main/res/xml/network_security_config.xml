<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!-- DEV : tout autoriser -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- PROD : uniquement HTTPS -->
    <!--
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    -->

</network-security-config>
